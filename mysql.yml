name: composes

services:
  mysql:
    image: mysql:8
    container_name: local_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: toor
    ports:
      - "3306:3306"
    volumes:
      - ./mysql/data:/var/lib/mysql